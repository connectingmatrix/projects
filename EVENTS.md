# giga-project Events

Runtime events must be explicit, owner/org scoped, and suitable for ClickHouse history where required. Current active process state must be emitted to live runtime registry rather than reconstructed from databases.
